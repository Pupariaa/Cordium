'use strict';

const { __cf } = eval(require(`current_filename`));
const { report, reportWarn, reportError } = console.createReports(__cf);

module.exports = {
    callback: async function(emoji) {
    }
}